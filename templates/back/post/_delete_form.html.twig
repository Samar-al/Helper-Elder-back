<div class="container">
    <form class="mt-3" method="post" action="{{ path('app_back_post_delete', {'id': post.id}) }}" onsubmit="return confirm('Etes-vous sûr de vouloir supprimer ce service?');">
        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ post.id) }}">
        <button class="btn btn-danger">Supprimer</button>
    </form>
</div>
