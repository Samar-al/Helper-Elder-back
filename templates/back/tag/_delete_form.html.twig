<div class="delete">
<form class="mt-3" method="post" action="{{ path('app_back_tag_delete', {'id': tag.id}) }}" onsubmit="return confirm('Etes-vous sûr de vouloir supprimer ce service?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tag.id) }}">
     <button class="btn btn-danger">{{ button_label|default('Supprimer') }}</button>
</form>
</div>